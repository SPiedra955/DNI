from src.dni import *
import pytest

@pytest.mark.check_document
def test_check_document():
    assert dni().checkDni() 
    
    assert dni().checkValidLetter()
    assert dni().checkDniLen()
    assert dni().getDniNumbers()